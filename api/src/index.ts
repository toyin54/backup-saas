// POST /auth/signup, POST /auth/login

// GET /projects, POST /projects

// GET /sources, POST /sources (store credentials encrypted)

// GET /destinations, POST /destinations

// GET /jobs, POST /jobs, PATCH /jobs/:id, DELETE /jobs/:id

// GET /jobs/:id/runs, POST /jobs/:id/run (manual trigger)

// GET /runs/:id/log (tail last N lines)

// POST /webhooks/stripe (billing events)